1. Pacotes do sistema e runtimes:
-> Docker Engine e containerd
-> Docker Compose Plugin (v2)
-> Utilitários de sistema: curl, git, unzip, ca-certificates, gnupg

2. Configurações de rede, permissões e serviços (systemd):
-> Adicionar usuário padrão (ex: ubuntu ou ec2-user) ao grupo docker
-> Habilitar o serviço principal do Docker no boot
-> [NOVO] Criar e habilitar o serviço iftech-app.service no Systemd para executar docker compose up -d no boot da EC2

3. Preparação dos artefatos de IA & Aplicação:
-> Clonagem do repositório para /opt/iftech
-> [NOVO] Ajustar o dono do diretório para o usuário padrão (chown -R ubuntu:ubuntu /opt/iftech) garantindo permissão no model_frozen.pt
-> Criação do arquivo .env para o Docker Compose (OLLAMA_URL, LLM_MODEL, BACKEND_URL)
-> Pré-build das imagens (docker compose build)
-> [NOVO] Inicializar temporariamente o contêiner do Ollama, executar o pull do modelo de IA (ex: phi3:mini) e persistir os dados no volume